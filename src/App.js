import React from 'react';
import Loader from './components/Loader';
import './App.css';

const App = () => {
    return (
        <div className="app-container">
            <Loader />
        </div>
    );
};

export default App;
